__pycache__/
*.pyc
.venv/
.pytest_cache/
.env
lfg_nfts.db
output_nft_*.png
metadata_*.json
.layer_cache/
generated/
images_*/
layers/**
!layers/
!layers/seasons.json
# Archived/dead code — kept on disk for reference, backed up at ~/linode-backup
# and tracked in issue #53. Untracked from the repo; also excluded from the gate.
legacy/
swap_records/
failed_db_records/
*.db.bak*
webapp.log
users.json
# root-level design mockups are ignored, but the Activity frontend IS source
*.html
!webapp/client/index.html
.mypy_cache/
.ruff_cache/
.remember/

# Legacy test trait art (superseded by layers/ on the CDN)
trait_layers/

# Generated audit reports
reports/

# Economy flow journals (runtime recovery records)
economy_records/

# Bulk mint job records (runtime recovery records, #215)
bulk_mint_jobs/

# On-chain NFT index DBs (regenerable via backfill)
onchain_*.db
onchain_*.db-*

# Bithomp collection CSV exports (source data, not code)
LFGO*.csv

# Per-network ledger history DBs (regenerable via backfill)
history_testnet.db*
history_mainnet.db*

# Network-aware app DB + rarity cache (regenerable; never commit)
lfg_nfts_*.db
rarity.db

# macOS finder metadata
.DS_Store
._.DS_Store
# macOS AppleDouble / resource-fork sidecars (never commit these)
._*

# Local archive of dead root clutter (backups, logs, mockups, source CSVs) —
# moved out of the repo root for tidiness; never committed.
.archive/
